# Impeccable: platform engine binary, fetched by the skill launcher on first run
**/skills/impeccable/scripts/bin/

# Claude Code local settings (machine-specific hook wiring)
.claude/settings.local.json

# Node
node_modules/

# Vite build output
dist/

# Impeccable capture evidence, regenerated per review round
.impeccable/review/
